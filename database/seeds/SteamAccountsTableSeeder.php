<?php

use Illuminate\Database\Seeder;

class SteamAccountsTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
//        $table->increments('id');
//        $table->integer('user_id')->unsigned();
//        $table->foreign('user_id')->references('id')->on('users');
//        $table->string('api_key');
//        //public data
//        $table->string('steam_id'); //"steamid" 64bit SteamID of the user
//        $table->string('name'); //"personaname"
//        $table->string('profile_url'); //"profileurl"
//        $table->string('avatar'); //"avatar": 32x32
//        $table->integer('status'); //"personastate": 0 - Offline, 1 - Online, 2 - Busy, 3 - Away, 4 - Snooze, 5 - looking to trade, 6 - looking to play.
//        $table->integer('visibility'); //"communityvisibilitystate": 1 - the profile is not visible to you (Private, Friends Only, etc), 3 - the profile is "Public"
//        $table->integer('profile_state'); //"profilestate": If set, indicates the user has a community profile configured (will be set to '1')
//        $table->timestamp('last_logoff'); //"lastlogoff": If set, indicates the user has a community profile configured (will be set to '1')
//        $table->string('comment_permission'); //"commentpermission": If set, indicates the profile allows public comments.
//        //private data
//        $table->string('real_name'); //"realname": The player's "Real Name", if they have set it.
//        $table->string('primary_group_id'); //"primaryclanid": The player's primary group, as configured in their Steam Community profile.
//        $table->timestamp('time_created'); //"timecreated": The time the player's account was created.
//        $table->string('game_id'); //"gameid": If the user is currently in-game, this value will be returned and set to the gameid of that game.
//        $table->string('gameserver_ip'); //"gameserverip": The ip and port of the game server the user is currently playing on, if they are playing on-line in a game using Steam matchmaking. Otherwise will be set to "0.0.0.0:0".
//        $table->string('loc_country_code'); //"loccountrycode": If set on the user's Steam Community profile, The user's country of residence, 2-character ISO country code
//        $table->string('loc_state_code'); //"locstatecode": If set on the user's Steam Community profile, The user's state of residence
//        $table->string('loc_city_id'); //"loccityid": An internal code indicating the user's city of residence. A future update will provide this data in a more useful way.
//        $table->timestamps();


    }
}
